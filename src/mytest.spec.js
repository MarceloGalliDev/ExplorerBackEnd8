it("result of them sum of 2 + 2 must be 4", () => {
    const a = 2;
    const b = 2;
    const result = a + b;

    expect(result).toEqual(4);
});
//it() recebe dois paramentros, descrição do teste, depois a função executora
//nos teste trabalhamos com expectativas, se minha espectativa for atingida quer dizer que passou no teste